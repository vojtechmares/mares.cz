---
import ErrorPage from "../features/error/ErrorPage.astro";

interface Props {
  error: unknown;
}

const { error } = Astro.props;
---

<ErrorPage status={500} detail={error instanceof Error ? error.message : undefined} />
